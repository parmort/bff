#pragma once

#include <curses.h>
#include <filesystem>
#include <string>
#include <vector>

namespace bff {

using std::string;
using std::vector;

using std::filesystem::path;
using std::filesystem::directory_entry;

} // namespace bff
