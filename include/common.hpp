#pragma once

#include <curses.h>
#include <filesystem>
#include <string>
#include <vector>

namespace bff {

using std::string;
using std::vector;

namespace fs = std::filesystem;

} // namespace bff
